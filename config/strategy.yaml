strategy:
  name: "FedAvg" # Type of strategy ["FedAvg", "FedProx", "FedNova", "FedOpt", "FedMA", "FedDF"]
  fraction_train_clients: 0.05
  fraction_eval_clients: 0.05
  min_available_clients: 3
  config_fit:
    lr: 0.01
    momentum: 0.9
    local_epochs: 1
    local_batch_size: 8
    local_val_batch_size: 16

  num_rounds: 10
  device: "cuda" # "cuda" or "cpu"
  total_gpus: 1
  total_cpus: 32